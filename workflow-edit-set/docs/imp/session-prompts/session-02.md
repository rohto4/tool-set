# Session 02 Prompt

## Name

CLI / Core 分離の下準備

## Start Prompt

このセッションでは `app.js` の責務分割方針整理に集中してください。UI の見た目調整は原則避け、状態管理、描画、入力、DSL、履歴のどこを切り出すかを定義してください。必要なら `site/core/` や `site/modules/` の作成方針を決めてください。完了条件は、UI 依存のない操作関数を切り出す対象一覧が明文化され、Knowledge Vault に判断理由が残っていることです。
